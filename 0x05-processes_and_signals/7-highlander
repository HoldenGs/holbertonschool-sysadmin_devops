#!/usr/bin/env bash
# Print a string indefinitely, can't be killed with SIGTERM
# shellcheck disable=SC2078
trap 'echo "I am invincible!!!"' TERM
while [ true ];
do
    echo "To infinity and beyond"
    sleep 2
done
